import { defineConfig } from 'vite';

// Cambia 'your-repository-name' por el nombre de tu repositorio en GitHub
export default defineConfig({
  base: '/portfolio/',  // Asegúrate de que el 'base' coincide con el nombre de tu repositorio
});